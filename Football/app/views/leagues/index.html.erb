
        <h1>A List of your Leagues</h1>

    <ul>
        <%=@user.leagues.each do |league|%>
    
            <%= content_tag(:p, (link_to "Look Inside Your League: #{league.name}", league_path(league)))%>

        <%end%>
    
    </ul>

        <%= link_to "Create New League", new_user_league_path(@user) %>
